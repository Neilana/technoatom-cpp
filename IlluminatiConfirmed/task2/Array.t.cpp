#include "gtest/gtest.h"
#include "Array.h"

using IlluminatiConfirmed::Array;

TEST(ArrayTest, CheckEmpty)
{
/*    Stack <int> stack1;
    bool isEmpty = stack1.empty();
    ASSERT_EQ(isEmpty, true);

    stack1.push(1);
    stack1.push(2);
    stack1.push(3);
    isEmpty = stack1.empty();
    ASSERT_EQ(isEmpty, false);

    stack1.pop();
    stack1.pop();
    stack1.pop();
    isEmpty = stack1.empty();
    ASSERT_EQ(isEmpty, true);
    */
}
